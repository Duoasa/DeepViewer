---
version: 0.2.3
build: 1
tag: v0.2.3
release_type: Public preview / Latest
status: Prepared
date: 2026-08-22
published: null
source_commit: d57c045342389f9f3ff3e1ba44758b07d0cfd477
specs: [DV-0015]
---

# DeepViewer 0.2.3（Build 1）

该候选版本将 DeepViewer 内置核心升级到官方 DeepSeek Harness `0.1.1-rc.2`
（提交 `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`），并适配内置订阅与预览插件、
rc.2 受控 UI 覆盖和独立的 DeepViewer mark/name 品牌槽位。

两个架构的最终 DMG 已完成 Developer ID 签名、Apple 公证、ticket staple、Gatekeeper、
磁盘镜像验证和只读挂载应用回读。本文随 0.2.3 产品图与 README 下载说明进入发布源码；
Git tag、GitHub Release 与远端资产回读完成后再将记录状态从 `Prepared` 更新为 `Released`。

## 发布资产

| 目标 | 文件 | 状态 | 大小 | SHA-256 |
| --- | --- | --- | --- | --- |
| macOS arm64 / Apple Silicon | `DeepViewer-0.2.3-macos-arm64.dmg` | Notarized / Upload pending | 555,495,477 bytes | `4c86ca24958f74f9e049d5a97bb34cb51415724188065f8fdd501b6ca47b8adb` |
| macOS x64 / Intel | `DeepViewer-0.2.3-macos-x64.dmg` | Notarized / Upload pending | 589,232,353 bytes | `1857891ae3b8a610656d7b6f77442e1aed6e7bfb6d5e57097c9b4669d552aec8` |
| 校验清单 | `SHA256SUMS.txt` | Upload pending | 196 bytes | `1dc431acd2eccd0f19e942222f9483a0d8a80ab11111f38345d596420dab2e43` |

## 构建与验证

- 从固定 rc.2 checkout 生成 9 个 vendor 与 227 个 DSH release-pack tarball；两个架构的
  Runtime、staging、应用目录和 DMG 均在候选封包前重新生成。
- 桌面 15 个测试文件 / 106 项测试、TypeScript、桌面 production build、DSH Host/Client/Web
  official build，以及 284 个 DSH GUI 测试文件（3998 passed / 1 skipped）通过。
- arm64 与 x64 应用各有 34 个 Mach-O 通过 Developer ID 严格嵌套签名验证；主程序架构
  分别回读为 `arm64` 与 `x86_64`。
- 两个包体的 ASAR 均只有 16 个允许列表入口，个人路径、凭据值和 Runtime 符号链接逃逸审计通过。
- 两个 DMG 均获得 Apple `Accepted`，完成 ticket staple/validate、`hdiutil verify`、Developer ID
  磁盘镜像签名、Gatekeeper 与只读挂载应用评估；应用图标资源与项目 `DeepViewer.icns` 内容一致。
- 维护者提供的 3064×1970 原始 PNG 按字节不变保存为 `Resources/DeepViewer-0.2.3.png`，并用于
  双语 README 顶图与 0.2.3 版本章节；SHA-256 为
  `8deceffeef445165b7878ec58aa13af89aaf8104284428be1ed2aa211839dcaf`。

## 插件状态

- `dsh-plugin-subscriptions@0.3.1`：staging 副本的四个 DSH peer 精确适配 rc.2，PC-001—PC-008
  自动检查与候选封包通过；PC-009 真实账户完整流程仍为 Pending Manual。
- `@deepviewer/dsh-plugin-preview@0.1.0`：八个 DSH Client peer 精确固定 rc.2，预览 RPC、路由、
  详情 UI、禁用降级与候选封包通过。

## 当前边界

- GitHub Release 创建和资产上传尚待执行；完成前 README 中的 `v0.2.3` 下载链接不会在远端可用。
- DV-0015 因订阅 PC-009 与其余维护者人工流程继续保持 `Implementing`，本候选记录不将规格
  提前标记为 `Verified` 或 `Released`。
- Runtime 包体积优化另立规格，不与本次 rc.2 兼容升级混合实施。

## 源码状态

- 发布分支 `codex/v0.2.3-dsh-rc2` 与远端 `main` 已包含签名候选代码；产品图、最终 README
  与公证证据将作为公开发布源码继续快进推送。
- 可执行候选源码提交为 `d57c045342389f9f3ff3e1ba44758b07d0cfd477`；后续提交只补充
  候选记录与远端回读证据，不改变签名产物输入。
