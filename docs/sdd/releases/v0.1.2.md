---
version: 0.1.2
tag: v0.1.2
release_type: Release candidate
status: Preparing
date: 2026-08-17
specs: [DV-0008, DV-0009]
---

# DeepViewer v0.1.2 候选版本

DeepViewer `v0.1.2` 当前处于发布准备阶段。本记录只描述已经进入候选源码的功能与已执行验证；
提交、tag、双架构 Runtime、签名、公证、DMG、SHA-256 和 GitHub Release 均须在实际完成后
回填，不能由历史 `v0.1.1` 资产推断。

## 候选版本状态

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 版本号 | Prepared | 桌面 manifest 设置为 `0.1.2` |
| 关联规格 | Prepared | DV-0008、DV-0009 |
| 维护者 UI 验收 | Pass | 2026-08-17 多轮截图反馈与里程碑确认 |
| 源码类型、测试与 production build | Pass | 覆盖同步/build、TypeScript、9 个测试文件/49 项测试和三段 Vite build 通过 |
| 封包隐私源码门禁 | Pass | staging 只复制声明文件与无空格的 Renderer 构建资产；最终 ASAR 逐路径 allowlist，并扫描个人路径、敏感文件名和环境凭据值 |
| Release commit / tag | Pending | 尚未提交或创建 `v0.1.2` tag |
| GitHub Release | Pending | 尚未创建或上传 |

## 候选更新内容

- 将 macOS 主窗口整理为侧栏与 Chat 两列结构，各列独立拥有系统安全区。
- 把模型使用统计移动到 Chat 顶部安全区居中显示，输入框在新会话、思考和完成状态保持
  `32px` 统一底部基准。
- 新增完整 Chat 画布居中的新会话欢迎内容：48px 半透明 DeepViewer 动画图形和本地化
  “让我们做点什么”文案。
- 侧栏使用受控内联 DeepViewer SVG，并完成浅色/深色、聚焦/失焦、原生磨砂材质、遮罩连续性、
  固定侧栏按钮和浅色原生窗口控件可读性处理。
- 新增可追踪的上游 UI 覆盖同步与重建机制，避免产品定制只存在于被忽略的 checkout。
- 建立开发、ARM 本地预览和正式双架构发布三个显式层级，开发数据与正式数据隔离。
- 正式和预览封包均强制同步受控覆盖，并使用文件级 staging allowlist，避免本地未跟踪文件、
  source map 或重复文件进入最终 ASAR。

## 计划发布资产

| 目标 | 文件 | 状态 | 大小 | SHA-256 |
| --- | --- | --- | --- | --- |
| macOS arm64 / Apple Silicon | `DeepViewer-0.1.2-macos-arm64.dmg` | Pending fresh build | Pending | Pending |
| macOS x64 / Intel | `DeepViewer-0.1.2-macos-x64.dmg` | Pending fresh build | Pending | Pending |
| 校验清单 | `SHA256SUMS.txt` | Pending | Pending | Pending |

## 发布门禁

- [ ] 从最终 release commit 全新同步受控上游覆盖并通过 production build。
- [ ] 分别全新构建 arm64 与 x64 Runtime、allowlist staging、应用和 DMG。
- [ ] 对两个架构执行个人路径、设置、凭据值、链接和包体隐私审计。
- [ ] 使用 Developer ID 对应用与 DMG 签名，并验证嵌套代码签名。
- [ ] 两个架构分别取得 Apple notarization `Accepted`，完成 staple 与 validate。
- [ ] 运行 Gatekeeper、`hdiutil verify`、挂载启动、架构与版本检查。
- [ ] 生成最终 SHA-256 清单，并记录本地文件大小与摘要。
- [ ] 维护者明确授权后创建 commit/tag、推送并发布 GitHub Release。
- [ ] 下载远端完整资产，确认远端大小和 SHA-256 与本地一致。
- [ ] 将本记录状态改为 `Released`，更新 README 下载入口和 Latest 标记。

## 当前边界

- 本记录不是公开发布声明，`v0.1.1` 仍是当前 Latest 和唯一有完整公开资产证据的版本。
- 本阶段未生成、复用或重命名任何历史 DMG，也未执行签名、公证或上传。
- DV-0006 的 reduced-motion 与 Runtime 失败态、DV-0003 的真实 Intel Mac 等既有待验收项不因
  `v0.1.2` 候选版本而自动通过。
