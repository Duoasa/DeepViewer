---
version: 0.1.1
tag: v0.1.1
release_type: Public pre-release
published: 2026-08-16
source_commit: 3ecfb426634619c7464b082ebaa3928a8b7e1db5
related_specs: [DV-0003, DV-0004, DV-0005, DV-0006]
---

# DeepViewer v0.1.1

DeepViewer `v0.1.1` 是首个完成 macOS 产品外壳与品牌启动体验的公开预览版。它提供可直接下载的 Apple Silicon 与 Intel 完整 DMG，并将“所有公开资产必须是无个人数据和凭据的全新构建”纳入强制发布门禁。

## 发布元数据

| 项目 | 值 |
| --- | --- |
| GitHub Release | [DeepViewer 0.1.1 — macOS UI Preview](https://github.com/Duoasa/DeepViewer/releases/tag/v0.1.1) |
| 发布状态 | Public、non-draft、pre-release |
| 发布时间 | 2026-08-16 06:27:37 +08:00 |
| Tag | `v0.1.1` |
| Tag / 源代码提交 | `3ecfb426634619c7464b082ebaa3928a8b7e1db5` |
| 构建输入提交 | `a1cd702`；与 tag 提交的 Git tree 均为 `68e0dd3e2190b4b347ade99e176bfc5bb57fe5ca` |
| 合并 PR | [#1 release: prepare DeepViewer 0.1.1](https://github.com/Duoasa/DeepViewer/pull/1) |

## 发布资产

| 平台与架构 | 文件 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| macOS arm64 / Apple Silicon | [DeepViewer-0.1.1-macos-arm64.dmg](https://github.com/Duoasa/DeepViewer/releases/download/v0.1.1/DeepViewer-0.1.1-macos-arm64.dmg) | 447,398,592 bytes | `3eea789d36458272cee469a80167d09badb1abea1723abd88f118da465d406b9` |
| macOS x64 / Intel | [DeepViewer-0.1.1-macos-x64.dmg](https://github.com/Duoasa/DeepViewer/releases/download/v0.1.1/DeepViewer-0.1.1-macos-x64.dmg) | 462,527,510 bytes | `f7b70f7fcdf8641f7228a2df42242e444688b8ac3ec1865c27029be9624dd561` |
| 校验清单 | [SHA256SUMS.txt](https://github.com/Duoasa/DeepViewer/releases/download/v0.1.1/SHA256SUMS.txt) | 196 bytes | `3354300bcde8ef5434dae3b2aa8bd4c370b71718da50303c5681b40f0a1e1fbc` |

GitHub API 报告三个资产均处于 `uploaded` 状态；远端 `sha256:` digest、大小与本地结果完全一致。发布页不包含临时分片或需要用户手动拼接的资产。

## 全新构建与隐私门禁

- arm64 与 x64 在各自封包前清理 Runtime、staging、`.app` 输出和旧 DMG。
- 应用只从包含 `.desktop`、`assets`、`package.json` 的 allowlist staging 目录封包；包管理器工作区元数据和历史输出不会进入 ASAR。
- Runtime 构建移除 pnpm 状态文件，并把构建机项目路径和主目录替换为稳定的非个人占位路径。
- 两个最终 `.app` 均通过 `release-audit.mjs`：检查 ASAR allowlist、敏感文件名、开发者绝对路径与当前环境中的高置信度凭据值；日志不打印秘密原文。
- 本次资产为 `0.1.1` 源码树生成的全新包，不复用或改名 `v0.0.1` 的 `.app`、Runtime、DMG 或校验清单。

## 验证范围

- `pnpm typecheck` 通过。
- 6 个测试文件、27 项测试全部通过。
- main、preload、renderer production build 通过。
- arm64 与 x64 主可执行文件分别确认为原生 `arm64`、`x86_64` Mach-O。
- 两个包的 `CFBundleDisplayName` 为 `DeepViewer`，版本为 `0.1.1`。
- 两个最终 DMG 均通过 `hdiutil verify`，完整文件 SHA-256 与 GitHub 远端 digest 一致。
- 依据 SDD 验证职责，本次发布收尾未由开发代理执行应用交互测试；维护者负责最终手动验收，并已反馈当前版本初步通过。

## 用户可见变化

- 原生 macOS 交通灯进入应用表面，侧栏完整收起、全屏按钮补位、全宽拖动区和 48px 顶部安全区与 Codex 行为对齐。
- 系统应用名、窗口名、Dock 与安装包身份统一为 `DeepViewer`，使用 macOS 26 图标源。
- 启动等待页显示居中的 DeepViewer Logo 与光标横线动效；插件加载阶段显示静止 Logo 和 `Loading Plugins...` 文字流光。
- 英文与简体中文 README 加入产品图、应用图标、架构下载和校验入口。

## 已知限制

- 应用未使用 Apple Developer ID 签名，也未经过 Apple 公证；macOS Gatekeeper 可能要求显式批准。
- x64 包已完成架构检查和 Apple Silicon + Rosetta 验证，但尚未在真实 Intel Mac 上验收。
- DV-0003 的无密钥流式任务、特殊路径/clean environment 与安全 UI 走查尚未全部完成。
- DV-0006 的减少动态效果和 Runtime 失败态仍等待维护者专项人工验收。
- Windows、自动更新和稳定版支持承诺不属于本版本。

## 规格状态判断

- DV-0004 与 DV-0005 的验收条件已通过，并随本公开版本进入 `Released`。
- DV-0003 已完成公开发布隐私门禁 AC-011，但其他验收条件仍未齐备，因此保持 `Implementing`。
- DV-0006 的主要启动/插件加载体验已获维护者初步验收；减少动态效果与失败态仍为 `Pending Manual`，因此保持 `Implementing`。
