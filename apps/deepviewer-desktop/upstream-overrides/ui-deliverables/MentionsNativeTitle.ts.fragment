return {
  open: () => {
    openDeepViewerPreviewOrFallback(
      resolveWorkspacePath(cwd, path),
      () => { openFile(path) },
    )
  },
  label: label(path),
  title: resolveWorkspacePath(cwd, path),
}
